<div class = "container">
  <div class = "row">
    <div class = "mx-auto">
      <h2>愚痴の間</h2><br><br>
      <p>思いを吐き出して落ち着きましょう</p>
    </div>
    <div class = "card w-50 mx-auto mt-4 shadow-lg">

      <% @complaints.each do |c| %>
        <tr>
          <p><%= c.title %></p>
          <p><%= c.body %></p>
          <%= link_to "削除", complaint_path(c.id), method: :delete %>
        </tr>


        <!--いいね機能-->
        <% if c.cheered_by?(current_user) %>
        <p>
          <%= button_to c.cheers.count, cheer_path(c), method: :delete, class: "glyphicon glyphicon-heart" %>お疲れ様！
          <%= button_to c.cheers.count, cheer_path(c), method: :delete, class: "glyphicon glyphicon-heart" %>

        <% else %>
          <%= button_to c.cheers.count, cheers_path(c), method: :post, class: "glyphicon glyphicon-heart-empty" %>お疲れ様！


        <% end %>

        </p>
      <% end %>
    </div>
    </div>
  </div>
