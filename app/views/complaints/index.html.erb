<div class = "container">
  <div class = "row">
    <div class = "mx-auto">
      <h2>皆様の愚痴</h2>
      <%= link_to "愚痴の間", room_complaints_path, class: "btn btn-primary" %>はこちらから
    </div>
    <div class = "card w-50 mx-auto mt-4 shadow-lg">

      <% @complaints.each do |c| %>

          <h6><%= c.title %></h>
          <p><%= c.body %></p>
          

        <% end %>


    </div>
  </div>
</div>
